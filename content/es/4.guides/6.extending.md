---
title: Extendiendo el Starter
description: Guía avanzada para extender y personalizar el comportamiento del starter
navigation:
  icon: i-lucide-code-2
---

Esta guía te muestra cómo extender el Auth Starter para casos de uso avanzados sin modificar el código fuente de la librería. El starter sigue el **Principio Abierto/Cerrado**: listo para usar desde el primer momento, pero abierto para extensión.

## Campos de Usuario Personalizados

El starter proporciona una forma flexible de agregar datos de usuario personalizados sin modificar el esquema de base de datos.

### Usando Metadatos JSONB

La entidad `Profile` incluye un campo `metadata` (JSONB en PostgreSQL) para almacenar información adicional de usuario:

```java
package com.yourapp.service;

import dev.ctlabs.starter.auth.domain.model.User;
import dev.ctlabs.starter.auth.domain.repository.UserRepository;
import org.springframework.stereotype.Service;

import java.util.HashMap;
import java.util.Map;
import java.util.UUID;

@Service
public class UserProfileService {

    private final UserRepository userRepository;

    public UserProfileService(UserRepository userRepository) {
        this.userRepository = userRepository;
    }

    public void addCustomFields(UUID userId, Map<String, Object> customData) {
        User user = userRepository.findById(userId)
            .orElseThrow(() -> new RuntimeException("User not found"));

        // Obtener metadatos existentes o crear nuevos
        Map<String, Object> metadata = user.getProfile().getMetadata();
        if (metadata == null) {
            metadata = new HashMap<>();
        }

        // Agregar campos personalizados
        metadata.put("birthdate", customData.get("birthdate"));
        metadata.put("phoneNumber", customData.get("phoneNumber"));
        metadata.put("address", customData.get("address"));
        metadata.put("preferences", Map.of(
            "theme", "dark",
            "language", "en",
            "notifications", true
        ));

        user.getProfile().setMetadata(metadata);
        userRepository.save(user);
    }

    public Map<String, Object> getCustomFields(UUID userId) {
        User user = userRepository.findById(userId)
            .orElseThrow(() -> new RuntimeException("User not found"));

        return user.getProfile().getMetadata();
    }
}
```

**Uso:**
```java
@PostMapping("/me/profile")
public void updateProfile(
        @RequestBody Map<String, Object> customData,
        @AuthenticationPrincipal UserDetails userDetails) {

    UUID userId = getUserId(userDetails);
    userProfileService.addCustomFields(userId, customData);
}
```

---

## Sobrescribiendo Beans

Reemplaza componentes predeterminados con tus propias implementaciones.

### Codificador de Contraseñas Personalizado

Reemplaza BCrypt con Argon2:

```java
package com.yourapp.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Primary;
import org.springframework.security.crypto.argon2.Argon2PasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;

@Configuration
public class CustomPasswordEncoderConfig {

    @Bean
    @Primary // Sobrescribir el BCryptPasswordEncoder predeterminado del starter
    public PasswordEncoder passwordEncoder() {
        return Argon2PasswordEncoder.defaultsForSpringSecurity_v5_8();
    }
}
```

### Generador de Token JWT Personalizado

Agrega claims personalizados a los tokens JWT extendiendo el `JwtService` del starter:

::warning
**Importante:** Debes extender `JwtService` o implementar TODOS sus métodos. El filtro de autenticación del starter requiere `extractAllClaims()` para validar tokens.
::

**Enfoque recomendado - Extender JwtService:**

```java
package com.yourapp.security;

import dev.ctlabs.starter.auth.autoconfigure.AuthProperties;
import dev.ctlabs.starter.auth.infrastructure.security.JwtService;
import org.springframework.context.annotation.Primary;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.stereotype.Service;

import java.util.HashMap;
import java.util.Map;

@Service
@Primary
public class CustomJwtService extends JwtService {

    public CustomJwtService(AuthProperties authProperties) {
        super(authProperties);
    }

    @Override
    public String generateToken(Map<String, Object> extraClaims, UserDetails userDetails) {
        // Agregar tus claims personalizados
        Map<String, Object> customClaims = new HashMap<>(extraClaims);

        // Ejemplo: Agregar datos de negocio personalizados
        customClaims.put("organizationId", getCurrentOrganizationId());
        customClaims.put("accountTier", getCurrentAccountTier());
        customClaims.put("featureFlags", getFeatureFlags());

        // Llamar al método padre para mantener la lógica de validación
        return super.generateToken(customClaims, userDetails);
    }

    private String getCurrentOrganizationId() {
        // Tu lógica para obtener organización del contexto
        return "org-123";
    }

    private String getCurrentAccountTier() {
        // Tu lógica para obtener nivel de cuenta
        return "premium";
    }

    private Map<String, Boolean> getFeatureFlags() {
        // Tu lógica para obtener banderas de funcionalidad
        return Map.of("advancedReports", true, "apiAccess", true);
    }
}
```

**Alternativa - Implementar todos los métodos:**

```java
@Service
@Primary
public class CustomJwtService {

    private final AuthProperties authProperties;

    public CustomJwtService(AuthProperties authProperties) {
        this.authProperties = authProperties;
    }

    public String generateToken(Map<String, Object> extraClaims, UserDetails userDetails) {
        Map<String, Object> claims = new HashMap<>(extraClaims);
        claims.put("email", userDetails.getUsername());
        claims.put("customField", "customValue");

        return Jwts.builder()
                .claims(claims)
                .subject(userDetails.getUsername())
                .issuedAt(new Date(System.currentTimeMillis()))
                .expiration(new Date(System.currentTimeMillis() +
                    authProperties.getJwt().getExpiration().toMillis()))
                .signWith(getSignInKey())
                .compact();
    }

    // ⚠️ REQUERIDO: Debes implementar esto o la autenticación fallará
    public Claims extractAllClaims(String token) {
        return Jwts.parser()
                .verifyWith(getSignInKey())
                .build()
                .parseSignedClaims(token)
                .getPayload();
    }

    private SecretKey getSignInKey() {
        byte[] keyBytes = Decoders.BASE64.decode(authProperties.getJwt().getSecretKey());
        return Keys.hmacShaKeyFor(keyBytes);
    }
}
```

::tip{title="Mejor Práctica"}
Extender `JwtService` es recomendado porque asegura que todos los métodos requeridos están implementados y la lógica de validación permanece consistente.
::

---

## Próximos Pasos

- [Guía de Personalización](/guides/customization) - Personaliza plantillas de email y unidades de tiempo
- [Guía RBAC](/guides/adding-roles) - Implementa control de acceso basado en roles
- [Gestión de Sesiones](/guides/session-management) - Gestiona sesiones de usuario
- [Referencia de API](/reference/rest-api) - Documentación completa de API

